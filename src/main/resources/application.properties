spring.application.name=spectraVisionPisip

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:postgresql://localhost:5432/spectraVision
spring.datasource.username=postgres
spring.datasource.password=admin

spring.jpa.show-sql=true

# Twilio WhatsApp (sandbox de pruebas). El auth-token NUNCA va aqui en texto plano
# porque este archivo esta trackeado en git: se toma de la variable de entorno TWILIO_AUTH_TOKEN.
